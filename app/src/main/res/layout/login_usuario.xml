<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:background="@color/colorBackgroud"
    android:layout_height="match_parent">

    <TextView
        android:text="Nome de Usuário:"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="7dp"
        android:layout_marginStart="7dp"
        android:layout_marginTop="64dp"
        android:id="@+id/tvUs"
        android:layout_below="@+id/logoLogin"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:background="@drawable/custom_edittext"
        android:ems="10"
        android:id="@+id/etNomeUs"
        android:layout_marginLeft="7dp"
        android:layout_marginStart="7dp"
        android:layout_alignBaseline="@+id/tvUs"
        android:layout_alignBottom="@+id/tvUs"
        android:layout_toRightOf="@+id/tvUs"
        android:layout_toEndOf="@+id/tvUs" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        app:srcCompat="@mipmap/logo_star_wars_app"
        android:id="@+id/logoLogin"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="Entrar"
        android:background="@drawable/background_button"
        android:textColor="#FFFFFF"
        android:id="@+id/btnLogin"
        android:textStyle="normal|bold"
        android:layout_below="@+id/etNomeUs"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp" />

</RelativeLayout>